/**
 * Host half of dsh-mission-control — deliberately empty.
 *
 * Mission Control is a pure-consumer client plugin (same posture as
 * ui-trajectory): it reads the runtime's session/projection surfaces and
 * renders into `shell.overlay`. No host services, no tools, no presets.
 */
export function apply() {}
